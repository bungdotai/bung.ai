{
  "name": "bung-ai",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:website": "cd apps/website && npm run dev",
    "build:website": "cd apps/website && npm run build"
  }
}
